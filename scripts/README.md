# 🚀 VPS Deploy Scripts

Скрипты для автоматического деплоя проекта на VPS.

## 📋 Подготовка

### 1. Настройте переменные в скриптах

Отредактируйте файлы скриптов:

```bash
# Замените на реальные значения
VPS_HOST="91.107.204.141"        # IP адрес вашего VPS
VPS_USER="root"                  # Пользователь для SSH
VPS_PORT="22"                    # SSH порт
PROJECT_DIR="/var/www/cv-project" # Директория проекта
GIT_REPO="https://github.com/mennekis/CV-project.git" # URL репозитория
```

### 2. Настройте SSH доступ

Убедитесь, что у вас есть SSH доступ к VPS:

```bash
# Тест подключения
ssh root@91.107.204.141
```

**Скрипты используют стандартный SSH ключ из `~/.ssh/id_ed25519`**

## 🎯 **Рекомендуемый порядок использования:**

### 1. **`vps_deploy_rsync.sh`** - **ОСНОВНОЙ СКРИПТ** ⭐

```bash
./scripts/vps_deploy_rsync.sh
```

**Идеальный для регулярных обновлений!**

**Что делает:**

-  ✅ Останавливает контейнеры (`docker-compose down`)
-  ✅ Синхронизирует все файлы через `rsync`
-  ✅ Запускает контейнеры (`docker-compose up -d --build`)
-  ✅ Проверяет статус и логи
-  ✅ Проверяет здоровье сервисов

**Когда использовать:** При каждом обновлении кода, темы, API

### 2. **`sync_theme.sh`** - Только для темы WordPress

```bash
./scripts/sync_theme.sh
```

**Что делает:**

-  ✅ Синхронизирует только тему WordPress
-  ✅ Устанавливает правильные права доступа
-  ✅ Перезапускает WordPress контейнер
-  ✅ Очищает кэш WordPress

**Когда использовать:** При изменении только темы WordPress

### 3. **`vps_clean_deploy.sh`** - Полная очистка VPS

```bash
./scripts/vps_clean_deploy.sh
```

**ВНИМАНИЕ**: Этот скрипт удалит ВСЕ Docker контейнеры, образы и данные!

**Что делает:**

-  ✅ Останавливает все Docker контейнеры
-  ✅ Удаляет все Docker образы и volumes
-  ✅ Очищает Docker систему
-  ✅ Удаляет старую директорию проекта
-  ✅ Устанавливает Docker и Docker Compose (если нужно)
-  ✅ Освобождает порты 80 и 443
-  ✅ Проверяет систему

**Когда использовать:** Только при первом деплое или полной переустановке

## 🔧 **Дополнительные скрипты:**

### 5. **`check_status.sh`** - Проверка статуса

```bash
./scripts/check_status.sh
```

**Что делает:**

-  ✅ Проверяет статус Docker контейнеров
-  ✅ Проверяет HTTPS и SSL
-  ✅ Проверяет редиректы
-  ✅ Проверяет заголовки безопасности

### 6. **`sync_ssl_certs.sh`** - Синхронизация SSL сертификатов

```bash
./scripts/sync_ssl_certs.sh
```

**Что делает:**

-  ✅ Создает локальную ssl директорию
-  ✅ Копирует сертификаты из ssl-configs/ в ssl/
-  ✅ Синхронизирует сертификаты с VPS
-  ✅ Проверяет успешность синхронизации

### 7. **`deploy_ssl.sh`** - Деплой SSL сертификатов

```bash
./scripts/deploy_ssl.sh
```

**Что делает:**

-  ✅ Загружает SSL сертификаты Cloudflare
-  ✅ Обновляет Nginx конфигурацию
-  ✅ Перезапускает Nginx

### 7. **`check_ssl_labs.sh`** - Проверка SSL Labs

```bash
./scripts/check_ssl_labs.sh
```

**Что делает:**

-  ✅ Проверяет рейтинг SSL Labs
-  ✅ Показывает результаты тестирования

### 8. **`compare_configs.sh`** - Сравнение конфигураций

```bash
./scripts/compare_configs.sh
```

**Что делает:**

-  ✅ Сравнивает локальные и удаленные конфигурации
-  ✅ Показывает различия в файлах

## 🚀 **Быстрые команды:**

### **Для ежедневных обновлений:**

```bash
# Обновить все файлы и перезапустить сервисы
./scripts/vps_deploy_rsync.sh
```

### **Для обновления только темы:**

```bash
# Обновить только тему WordPress
./scripts/sync_theme.sh
```

### **Для проверки статуса:**

```bash
# Проверить статус всех сервисов
./scripts/check_status.sh
```

### **Для деплоя SSL:**

```bash
# Обновить SSL сертификаты
./scripts/deploy_ssl.sh
```

## ⚙️ Настройка после деплоя

### 1. Отредактируйте .env на VPS

```bash
ssh root@91.107.204.141
cd /var/www/cv-project
nano .env
```

**Обязательно заполните:**

-  Пароли для баз данных
-  `TELEGRAM_BOT_TOKEN`
-  `TELEGRAM_CHAT_ID`
-  `WAYFORPAY_MERCHANT_ACCOUNT`
-  `WAYFORPAY_SECRET_KEY`

### 2. Перезапустите проект

```bash
docker-compose down
docker-compose up -d
```

### 3. Настройте домен в Cloudflare

-  Добавьте A-запись: `seafarer-cv.com` → `91.107.204.141`
-  Включите SSL/TLS
-  Настройте проксирование

## 📝 **Примеры использования:**

### **Сценарий 1: Обновление темы WordPress**

```bash
# 1. Внесли изменения в тему локально
# 2. Запускаем синхронизацию темы
./scripts/sync_theme.sh

# 3. Проверяем результат
./scripts/check_status.sh
```

### **Сценарий 2: Обновление API и темы**

```bash
# 1. Внесли изменения в backend и тему
# 2. Запускаем полный деплой
./scripts/vps_deploy_rsync.sh

# 3. Проверяем SSL
./scripts/check_ssl_labs.sh
```

### **Сценарий 3: Первый деплой на новый VPS**

```bash
# 1. Полная очистка VPS
./scripts/vps_clean_deploy.sh

# 2. Деплой проекта
./scripts/vps_deploy.sh

# 3. Настройка SSL
./scripts/deploy_ssl.sh
```

## 🔧 Полезные команды

### На VPS:

```bash
# Просмотр логов
docker-compose logs -f

# Перезапуск сервисов
docker-compose restart

# Остановка проекта
docker-compose down

# Обновление проекта
git pull
docker-compose up -d --build

# Проверка статуса
docker-compose ps

# Вход в контейнер
docker-compose exec backend sh
docker-compose exec wordpress bash
```

### Проверка здоровья:

```bash
# WordPress API
curl http://91.107.204.141/wp-json/

# Backend API
curl http://91.107.204.141/api/health

# Сайт
curl http://91.107.204.141
```

## 💡 **Рекомендации по использованию:**

### **Для ежедневной работы:**

-  ✅ Используйте `vps_deploy_rsync.sh` - он самый надежный
-  ✅ Всегда проверяйте статус после деплоя: `./scripts/check_status.sh`
-  ✅ При проблемах с темой используйте `sync_theme.sh`

### **Для безопасности:**

-  ⚠️ Никогда не используйте `vps_clean_deploy.sh` на продакшене без
   необходимости
-  ⚠️ Всегда делайте бэкапы перед большими изменениями
-  ⚠️ Проверяйте .env файл перед деплоем

### **Для оптимизации:**

-  🚀 Используйте `sync_theme.sh` для быстрых обновлений темы
-  🚀 Используйте `vps_deploy_rsync.sh` для полных обновлений
-  🚀 Регулярно проверяйте SSL: `./scripts/check_ssl_labs.sh`

## 🚨 Устранение проблем

### Проблема: Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose logs

# Проверьте .env файл
cat .env

# Пересоберите образы
docker-compose build --no-cache
```

### Проблема: Порты заняты

```bash
# Проверьте занятые порты
netstat -tuln | grep :80
netstat -tuln | grep :443

# Остановите Apache/Nginx
systemctl stop apache2
systemctl stop nginx
```

### Проблема: Нет места на диске

```bash
# Очистите Docker
docker system prune -af
docker volume prune -f

# Проверьте место
df -h
```

### Проблема: Файлы не обновляются

```bash
# Проверьте права доступа
ls -la /var/lib/docker/volumes/cv-project_wordpress_data/_data/wp-content/themes/cv-theme/

# Перезапустите контейнеры
docker-compose restart

# Очистите кэш WordPress
docker-compose exec -T wp_db mysql -u root -p'password' -e "USE wp_production; DELETE FROM wp_options WHERE option_name LIKE '%_transient_%';"
```

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус: `docker-compose ps`
3. Проверьте .env файл
4. Убедитесь, что все порты свободны
5. Используйте `./scripts/check_status.sh` для диагностики

## 🎯 **Краткое резюме:**

### **Основные скрипты:**

-  **`vps_deploy_rsync.sh`** - для ежедневных обновлений (останавливает →
   синхронизирует → запускает)
-  **`sync_theme.sh`** - для быстрых обновлений темы WordPress
-  **`check_status.sh`** - для проверки статуса всех сервисов

### **Специальные скрипты:**

-  **`vps_clean_deploy.sh`** - для полной очистки VPS (только при первом деплое)
-  **`deploy_ssl.sh`** - для обновления SSL сертификатов
-  **`check_ssl_labs.sh`** - для проверки SSL рейтинга

### **Рекомендуемый workflow:**

1. Внесли изменения локально
2. Запустили `./scripts/vps_deploy_rsync.sh`
3. Проверили `./scripts/check_status.sh`
4. Готово! 🚀

---

**💡 Помните:** `vps_deploy_rsync.sh` - ваш лучший друг для надежных обновлений!
