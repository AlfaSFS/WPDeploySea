# 🚀 WayForPay Advanced Price Override Script

## 🎯 Назначение:

Продвинутый скрипт для подмены цен в объектах товаров, формах, localStorage,
sessionStorage и AJAX запросах.

## 🔍 Что подменяет:

### ✅ Объекты товаров:

```javascript
// Подменяет такие объекты:
{
  id: "#1",
  name: "Торговий флот",
  price: 10,  // ← станет 1
  group: "Розсилка CV"
}
```

### ✅ Все типы данных:

-  **Поля ввода:** `input[name*="price"]`
-  **Отображаемые цены:** элементы с классом `price`
-  **Скрытые поля:** `input[type="hidden"]`
-  **localStorage:** все объекты с полем `price`
-  **sessionStorage:** все объекты с полем `price`
-  **Глобальные переменные:** `window.cartData`, `window.orderData`
-  **Формы:** все поля с именем содержащим `price`
-  **AJAX запросы:** перехватывает и модифицирует ответы

## 🚀 Как использовать:

### Метод 1: Через консоль браузера

1. **Откройте** страницу с товарами
2. **Нажмите F12** (DevTools)
3. **Вставьте код** из `wfp-advanced-override.js`
4. **Нажмите Enter**

### Метод 2: Автоматический запуск

Скрипт автоматически:

-  **Запускается** при загрузке
-  **Повторяется** каждые 2 секунды
-  **Мониторит** изменения в DOM
-  **Перехватывает** AJAX запросы

## 🎯 Кнопки управления:

### 💰 Подменить на 1

-  **Функция:** Подменяет все найденные цены на 1
-  **Область:** Формы, localStorage, sessionStorage, глобальные переменные
-  **Повтор:** Можно нажимать многократно

### 👁️ Мониторинг

-  **Функция:** Показывает текущие данные
-  **Вывод:** В консоль все найденные объекты и формы
-  **Отладка:** Помогает понять структуру данных

## 🔍 Пример работы:

### До подмены:

```javascript
// В localStorage
{
  "cart": [
    {
      "id": "#1",
      "name": "Торговий флот",
      "price": 10,  // ← оригинальная цена
      "group": "Розсилка CV"
    }
  ]
}
```

### После подмены:

```javascript
// В localStorage
{
  "cart": [
    {
      "id": "#1",
      "name": "Торговий флот",
      "price": 1,   // ← подмененная цена
      "group": "Розсилка CV"
    }
  ]
}
```

## 🛠️ Дополнительные функции:

### Доступные в консоли:

```javascript
// Ручная подмена
findAndOverridePrices();

// Активация перехвата AJAX
interceptAjaxRequests();

// Проверка данных
console.log(window.cartData);
console.log(Object.keys(localStorage));
```

### Мониторинг в реальном времени:

```javascript
// Просмотр всех данных
console.log({
   localStorage: Object.keys(localStorage),
   sessionStorage: Object.keys(sessionStorage),
   cartData: window.cartData,
   orderData: window.orderData,
});
```

## 📊 Ожидаемые результаты:

### ✅ После подмены:

-  **Все цены:** `1` (вместо оригинальных)
-  **Объекты товаров:** `price: 1`
-  **Формы:** поля с ценой = `1`
-  **localStorage:** обновленные объекты
-  **AJAX:** модифицированные ответы

### ✅ Автоматическая работа:

-  **Каждые 2 секунды:** проверка и подмена
-  **При изменениях DOM:** автоматическая подмена
-  **При AJAX запросах:** перехват и модификация

## ⚠️ Важные замечания:

### 🚨 Безопасность:

-  **Это реальные деньги** (1 EUR)
-  **Подменяет все цены** на странице
-  **Работает с AJAX** запросами

### 🔍 Отладка:

-  **Откройте консоль** для логов
-  **Используйте кнопку "👁️ Мониторинг"** для проверки
-  **Проверьте Network** для AJAX запросов

### 💾 Персистентность:

-  **Работает с localStorage** и sessionStorage
-  **Автоматически применяется** при изменениях
-  **Перехватывает AJAX** запросы

---

**Готово к продвинутому тестированию!** 🚀
