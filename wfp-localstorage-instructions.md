# 💾 WayForPay localStorage Price Override Script

## 🎯 Назначение:

Скрипт для подмены цены через localStorage - более надежный способ, так как
данные сохраняются между перезагрузками страницы.

## 🚀 Как использовать:

### Метод 1: Через консоль браузера

1. **Откройте** страницу с формой оплаты WayForPay
2. **Нажмите F12** (открыть DevTools)
3. **Перейдите на вкладку Console**
4. **Скопируйте и вставьте** весь код из файла `wfp-localstorage-override.js`
5. **Нажмите Enter** для выполнения

### Метод 2: Через Bookmarklet

```javascript
javascript: (function () {
   var script = document.createElement("script");
   script.src =
      "data:text/javascript;base64," +
      btoa(`
// Вставьте сюда код из wfp-localstorage-override.js
`);
   document.head.appendChild(script);
})();
```

## 🎯 Что делает скрипт:

### ✅ localStorage функции:

-  **Сохраняет оригинальные цены** в localStorage
-  **Устанавливает подмену** через localStorage
-  **Автоматически применяет** при перезагрузке
-  **Восстанавливает оригинальные** цены

### ✅ Кнопки управления:

-  **💰 Подменить на 1 EUR** - активирует подмену
-  **🔄 Восстановить** - возвращает оригинальные цены
-  **📊 Статус** - показывает текущее состояние

### ✅ Автоматическое применение:

-  **При загрузке страницы** проверяет localStorage
-  **Автоматически применяет** сохраненную подмену
-  **Мониторит изменения** в DOM

## 🔍 Пример использования:

### 1. Откройте форму оплаты:

```
https://secure.wayforpay.com/pay
```

### 2. Запустите скрипт:

```javascript
// Вставьте код из wfp-localstorage-override.js
```

### 3. Используйте кнопки:

-  **Нажмите "💰 Подменить на 1 EUR"**
-  **Цена станет:** `1.00 EUR`
-  **Данные сохранятся** в localStorage

### 4. Перезагрузите страницу:

-  **Подмена сохранится** автоматически
-  **Цена останется:** `1.00 EUR`
-  **Оригинальные цены** сохранены

### 5. Оплатите:

-  **Используйте реальную карту**
-  **Сумма будет:** 1 EUR
-  **Безопасно для тестирования**

## 📊 Ожидаемые результаты:

### ✅ После подмены:

-  **Отображаемая цена:** `1.00 EUR`
-  **localStorage:** `wfp_override_active: "true"`
-  **localStorage:** `wfp_override_price: "1.00"`
-  **localStorage:** `wfp_original_prices: {...}`

### ✅ После перезагрузки:

-  **Подмена сохраняется** автоматически
-  **Цена остается:** `1.00 EUR`
-  **Кнопки управления** доступны

### ✅ После восстановления:

-  **Оригинальные цены** возвращены
-  **localStorage:** `wfp_override_active: "false"`
-  **Подмена отключена**

## 🛠️ Дополнительные функции:

### Доступные в консоли:

```javascript
// Подмена через localStorage
overridePriceViaStorage();

// Восстановление оригинальных цен
restoreOriginalPrices();

// Применение подмены
applyPriceOverride();

// Проверка localStorage
localStorage.getItem("wfp_override_active");
localStorage.getItem("wfp_override_price");
localStorage.getItem("wfp_original_prices");
```

### Проверка localStorage:

```javascript
// Просмотр всех данных подмены
console.log({
   active: localStorage.getItem("wfp_override_active"),
   price: localStorage.getItem("wfp_override_price"),
   currency: localStorage.getItem("wfp_override_currency"),
   original: localStorage.getItem("wfp_original_prices"),
});
```

## 🔄 Управление данными:

### Очистка localStorage:

```javascript
// Полная очистка данных подмены
localStorage.removeItem("wfp_override_active");
localStorage.removeItem("wfp_override_price");
localStorage.removeItem("wfp_override_currency");
localStorage.removeItem("wfp_original_prices");
localStorage.removeItem("wfp_payment_data");
```

### Ручная установка:

```javascript
// Установка подмены вручную
localStorage.setItem("wfp_override_active", "true");
localStorage.setItem("wfp_override_price", "1.00");
localStorage.setItem("wfp_override_currency", "EUR");
```

## ⚠️ Важные замечания:

### 🚨 Безопасность:

-  **Это реальные деньги** (1 EUR)
-  **localStorage сохраняет** данные между сессиями
-  **WayForPay получит** правильную сумму

### 🔍 Отладка:

-  **Откройте консоль** для логов
-  **Проверьте Application → Local Storage** в DevTools
-  **Используйте кнопку "📊 Статус"** для проверки

### 💾 Персистентность:

-  **Данные сохраняются** между перезагрузками
-  **Автоматически применяются** при загрузке
-  **Можно восстановить** оригинальные цены

---

**Готово к надежному тестированию с localStorage!** 🚀
