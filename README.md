# CV Project

## 🚀 WordPress + Node.js + WayForPay E-commerce Platform

Полнофункциональная платформа для создания и продажи CV (резюме) с интеграцией
платежной системы WayForPay, уведомлениями в Telegram и современным
веб-интерфейсом.

### ✨ Основные возможности

-  🎨 **WordPress фронтенд** с кастомной темой `cv-theme`
-  ⚡ **Node.js API** для обработки заказов и файлов
-  💳 **WayForPay интеграция** для приема платежей
-  📱 **Telegram уведомления** о заказах и платежах
-  📁 **Загрузка файлов** резюме с автоматической отправкой
-  🔒 **SSL/HTTPS** через Cloudflare
-  🐳 **Docker Compose** для простого развертывания
-  📊 **Отдельная БД** для заказов (MariaDB)

---

## **📁 Структура проекта**

```
CV-project/
├── backend/                    # Node.js API сервер
│   ├── src/
│   │   ├── index.js           # Основной API сервер
│   │   ├── db.js              # Подключение к БД
│   │   ├── migrate.js         # Миграции БД
│   │   ├── telegram.js       # Telegram интеграция
│   │   └── payments/
│   │       ├── wayforpay.js   # WayForPay обработчик
│   │       └── index.js      # Экспорт модулей
│   ├── uploads/              # Загруженные файлы
│   └── Dockerfile
├── wp-content/
│   └── themes/
│       └── cv-theme/         # WordPress тема
├── nginx/
│   └── conf.d/               # Nginx конфигурации
├── scripts/                  # Скрипты деплоя и управления
├── ssl-configs/             # SSL сертификаты
├── docker-compose.yml       # Docker конфигурация
├── env.example              # Пример переменных окружения
├── env.production.example   # Продакшн переменные
└── README.md               # Документация
```

---

## **🔧 Требования**

-  **Docker & Docker Compose** (основное требование)
-  **Node.js 18+** (для локальной разработки)
-  **Git** (для контроля версий)
-  **WayForPay аккаунт** (для платежей)
-  **Telegram Bot** (для уведомлений)

---

## **🚀 Быстрый старт (локально)**

### 1. Клонирование проекта

```bash
git clone https://github.com/mennekis/CV-project.git
cd CV-project
```

### 2. Настройка переменных окружения

```bash
# Скопируйте пример конфигурации
cp env.example .env

# Отредактируйте .env файл
nano .env
```

**Основные переменные для локальной разработки:**

```env
# ===== WordPress Database =====
WP_DB_NAME=wp_local
WP_DB_USER=wp_user
WP_DB_PASS=wp_pass
WP_DB_ROOT=rootpass

# ===== Orders Database =====
ORD_DB_NAME=orders_local
ORD_DB_USER=orders_user
ORD_DB_PASS=orders_pass
ORD_DB_ROOT=rootpass2

# ===== WordPress Configuration =====
WP_PORT=8080
WP_URL=http://localhost:8080
WP_ADMIN_USER=admin
WP_ADMIN_PASS=admin123
WP_ADMIN_EMAIL=admin@example.com

# ===== Telegram Bot =====
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# ===== WayForPay Configuration (TEST MODE) =====
WAYFORPAY_ACCOUNT=test_merch_n1
WAYFORPAY_SECRET_KEY=flk3409refn54t54t*FNJRET
WAYFORPAY_DOMAIN=http://localhost:8080
WAYFORPAY_API_URL=https://secure.wayforpay.com/pay
WAYFORPAY_SERVICE_URL=http://localhost:8080/api/payments/wayforpay/callback
WAYFORPAY_RETURN_URL=http://localhost:8080/payment-success
WAYFORPAY_CURRENCY=UAH
EUR2UAH=40

# ===== CORS Configuration =====
ALLOWED_ORIGINS=http://localhost:8080
```

### 3. Запуск проекта

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps
```

### 4. Настройка WordPress

1. **WordPress доступен на:** http://localhost:8080
2. **Выполните стандартную установку WP:**
   -  Выберите язык (русский/украинский)
   -  Создайте админ аккаунт
   -  Установите пароль
3. **Активируйте тему `cv-theme`** через админку
4. **Настройте статическую главную страницу**

### 5. Запуск миграций базы данных

```bash
# Создание таблиц для заказов
docker-compose exec backend node src/migrate.js
```

### 6. Проверка работы

```bash
# Проверка API
curl http://localhost:8080/api/health

# Проверка WordPress
curl http://localhost:8080/wp-json/

# Проверка заказов
curl http://localhost:8080/api/orders
```

---

## **⚡ Node.js Backend API**

### 📋 Основные endpoints

| Метод  | Endpoint                           | Описание                      |
| ------ | ---------------------------------- | ----------------------------- |
| `POST` | `/api/orders`                      | Создание нового заказа        |
| `GET`  | `/api/orders/:id`                  | Получение заказа по ID        |
| `GET`  | `/api/orders`                      | Список заказов (с пагинацией) |
| `POST` | `/api/payments/wayforpay/callback` | Callback от WayForPay         |
| `GET`  | `/api/health`                      | Проверка здоровья API         |

### 📝 Структура заказа

**Новый формат (рекомендуемый):**

```json
{
   "email": "client@example.com",
   "amount": 100,
   "currency": "EUR",
   "items": [
      {
         "name": "CV Writing Service",
         "price": 100,
         "count": 1
      }
   ],
   "clientEmail": "client@example.com",
   "first_name": "Иван",
   "last_name": "Петров",
   "phone": "+380123456789"
}
```

**Классический формат (обратная совместимость):**

```json
{
   "name": "Иван",
   "surname": "Петров",
   "email": "client@example.com",
   "phone": "+380123456789",
   "total_amount": 100,
   "currency": "EUR",
   "cart": "[{\"name\":\"CV Writing\",\"price\":100}]",
   "messengers": ["telegram", "whatsapp"],
   "no-wish": "Нет пожеланий",
   "comment": "Дополнительный комментарий"
}
```

### 📁 Обработка файлов

-  **Загрузка:** Файлы сохраняются в `backend/uploads/`
-  **База данных:** Путь к файлу записывается в поле `cv_filename`
-  **Telegram:** Файлы автоматически отправляются после успешной оплаты
-  **Поддерживаемые форматы:** PDF, DOC, DOCX, TXT

---

## **💳 WayForPay Интеграция**

### 🔧 Настройка WayForPay

1. **Регистрация:** Зарегистрируйтесь на [WayForPay](https://wayforpay.com/)
2. **Получите данные:**
   -  `merchantAccount` - ID мерчанта
   -  `secretKey` - секретный ключ
   -  `domainName` - ваш домен

### 🧪 Тестовые данные

Для локального тестирования используйте:

```env
WAYFORPAY_ACCOUNT=test_merch_n1
WAYFORPAY_SECRET_KEY=flk3409refn54t54t*FNJRET
WAYFORPAY_DOMAIN=http://localhost:8080
WAYFORPAY_API_URL=https://secure.wayforpay.com/pay
WAYFORPAY_SERVICE_URL=http://localhost:8080/api/payments/wayforpay/callback
WAYFORPAY_RETURN_URL=http://localhost:8080/payment-success
WAYFORPAY_CURRENCY=UAH
EUR2UAH=40
```

### 🔄 Flow оплаты

1. **Создание заказа:** Клиент заполняет форму → `POST /api/orders`
2. **Генерация платежных данных:** Backend создает данные для WayForPay
3. **Перенаправление на оплату:** Клиент перенаправляется на WayForPay
4. **Обработка результата:** WayForPay отправляет callback на
   `/api/payments/wayforpay/callback`
5. **Обновление статуса:** Заказ переходит в статус "paid"
6. **Уведомления:** Отправка в Telegram с файлами

### 🔐 Безопасность

-  ✅ **HMAC-MD5 подписи** проверяются для всех callback'ов
-  ✅ **CORS настроен** для разрешенных доменов
-  ✅ **Валидация данных** на всех этапах

---

## **📱 Telegram Интеграция**

### 🤖 Настройка бота

1. **Создайте бота через [@BotFather](https://t.me/botfather)**
2. **Получите токен бота**
3. **Узнайте ID чата для уведомлений**

### 📬 Уведомления

-  **При создании заказа:** Отправляется полная информация о заказе
-  **После успешной оплаты:** Отправляется подтверждение с файлами резюме
-  **При ошибках:** Отправляются уведомления об ошибках

### 🔧 Тестирование Telegram

```bash
# Тест отправки сообщения
node test-telegram-ping.js
```

---

## **🚀 Развертывание на VPS**

### 📋 Подготовка VPS

**Рекомендуемые характеристики:**

-  **RAM:** 2GB+
-  **CPU:** 2+ ядра
-  **Диск:** 20GB+ SSD
-  **OS:** Ubuntu 20.04+ / Debian 11+

### 🔧 Автоматические скрипты деплоя

Проект включает полный набор скриптов для автоматического развертывания:

#### **Основные скрипты:**

| Скрипт                | Назначение          | Когда использовать              |
| --------------------- | ------------------- | ------------------------------- |
| `vps_deploy_rsync.sh` | **Основной деплой** | Ежедневные обновления           |
| `sync_theme.sh`       | Обновление темы     | Изменения только в WordPress    |
| `vps_clean_deploy.sh` | Полная очистка      | Первый деплой или переустановка |
| `check_status.sh`     | Проверка статуса    | Диагностика проблем             |

#### **🚀 Быстрый деплой:**

```bash
# 1. Первый деплой (полная очистка)
./scripts/vps_clean_deploy.sh

# 2. Обычные обновления
./scripts/vps_deploy_rsync.sh

# 3. Проверка статуса
./scripts/check_status.sh
```

### ⚙️ Настройка продакшн переменных

```bash
# Скопируйте продакшн конфигурацию
cp env.production.example .env

# Отредактируйте .env файл
nano .env
```

**Примечание:** На VPS используется `env.production.example` как основа для
`.env` файла.

**Актуальные переменные из `env.production.example`:**

```env
# ===== WordPress Database =====
WP_DB_NAME=wp_production
WP_DB_USER=wp_user
WP_DB_PASS=your_secure_wp_password
WP_DB_ROOT=your_secure_root_password

# ===== Orders Database =====
ORD_DB_NAME=orders_production
ORD_DB_USER=orders_user
ORD_DB_PASS=your_secure_orders_password
ORD_DB_ROOT=your_secure_root_password

# ===== WordPress Configuration =====
WP_URL=https://seafarer-cv.com
WP_ADMIN_USER=admin
WP_ADMIN_PASS=your_admin_password
WP_ADMIN_EMAIL=admin@seafarer-cv.com

# ===== Telegram Bot =====
TELEGRAM_BOT_TOKEN=8361222877:AAFmtxkFkoXu-htgNna1U_PVLhsPpFJE4qE
TELEGRAM_CHAT_ID=417042050

# ===== WayForPay Configuration (PRODUCTION MODE) =====
WFP_MODE=PROD
WAYFORPAY_ACCOUNT=seafarer_cv_com1
WAYFORPAY_SECRET_KEY=225a1feaab45b86a2211a11c6c071ade86c91b71
WAYFORPAY_DOMAIN=seafarer-cv.com
WAYFORPAY_API_URL=https://secure.wayforpay.com/pay
WAYFORPAY_SERVICE_URL=https://seafarer-cv.com/api/payments/wayforpay/callback
WAYFORPAY_RETURN_URL=https://seafarer-cv.com/payment-success
WAYFORPAY_CURRENCY=EUR
# EUR2UAH=40  # Удалено для PROD

# ===== CORS Configuration =====
ALLOWED_ORIGINS=https://seafarer-cv.com

# ===== Port Configuration =====
WP_PORT=80
```

**⚠️ Важно:**

-  Замените плейсхолдеры (`your_secure_*_password`) на реальные пароли перед
   развертыванием
-  На VPS уже настроены реальные пароли в файле `.env`
-  Telegram токен и WayForPay данные уже актуальны

### 🔒 SSL/HTTPS настройка

```bash
# Деплой SSL сертификатов
./scripts/deploy_ssl.sh

# Проверка SSL рейтинга
./scripts/check_ssl_labs.sh
```

### 🌐 Cloudflare настройка

1. **Добавьте домен в Cloudflare**
2. **Настройте A-запись:** `seafarer-cv.com` → `91.107.204.141`
3. **Включите SSL/TLS** (Full mode)
4. **Настройте проксирование**
5. **Загрузите SSL сертификаты** в `ssl-configs/`

---

## **🧪 Тестирование**

### 🔍 Локальное тестирование

```bash
# Проверка API здоровья
curl http://localhost:8080/api/health

# Создание тестового заказа
curl -X POST http://localhost:8080/api/orders \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "amount": 100,
    "currency": "EUR",
    "items": [{"name": "CV Writing", "price": 100, "count": 1}],
    "first_name": "Test",
    "last_name": "User"
  }'

# Проверка списка заказов
curl http://localhost:8080/api/orders
```

### 💳 Тестирование WayForPay

1. **Создайте заказ через форму**
2. **Проверьте перенаправление на WayForPay**
3. **Используйте тестовые карты:**
   -  **Успешная оплата:** `4000000000000002`
   -  **Отклоненная оплата:** `4000000000000069`
4. **Проверьте callback обработку**
5. **Убедитесь в получении уведомлений в Telegram**

### 📱 Тестирование Telegram

```bash
# Тест отправки сообщения
node test-telegram-ping.js

# Проверка в Telegram чате
# Должно прийти сообщение с информацией о тесте
```

---

## **📊 Мониторинг и логи**

### 📋 Просмотр логов

```bash
# Логи всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f nginx
docker-compose logs -f wordpress
docker-compose logs -f orders_db
```

### 🔍 Проверка статуса

```bash
# Статус контейнеров
docker-compose ps

# Проверка здоровья сервисов
curl http://localhost:8080/api/health
curl http://localhost:8080/wp-json/

# Проверка базы данных
docker-compose exec orders_db mysql -u orders_user -porders_pass orders_production -e 'SELECT COUNT(*) FROM orders;'
```

### 🚨 Диагностика проблем

```bash
# Проверка статуса на VPS
./scripts/check_status.sh

# Проверка SSL
./scripts/check_ssl_labs.sh

# Сравнение конфигураций
./scripts/compare_configs.sh
```

---

## **🔒 Безопасность**

### ✅ Реализованные меры безопасности

-  **🔐 Все пароли** в переменных окружения
-  **🌐 CORS настроен** для разрешенных доменов
-  **🔑 WayForPay подписи** проверяются HMAC-MD5
-  **📁 Файлы загружаются** в изолированную директорию
-  **🛡️ Nginx блокирует** доступ к системным файлам
-  **🔒 SSL/HTTPS** через Cloudflare
-  **🚫 Защита от SQL инъекций** через prepared statements
-  **📝 Валидация данных** на всех этапах

### 🛡️ Рекомендации по безопасности

1. **Регулярно обновляйте пароли** в `.env`
2. **Используйте сильные пароли** для баз данных
3. **Настройте firewall** на VPS
4. **Регулярно создавайте бэкапы** баз данных
5. **Мониторьте логи** на предмет подозрительной активности

---

## **📚 Дополнительная документация**

### 📖 Специализированные руководства

-  **[API_DOCUMENTATION.md](API_DOCUMENTATION.md)** - Полная документация API
-  **[DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)** - Подробное руководство по
   деплою
-  **[CLOUDFLARE_SETUP.md](CLOUDFLARE_SETUP.md)** - Настройка Cloudflare
-  **[TELEGRAM_SETUP.md](TELEGRAM_SETUP.md)** - Настройка Telegram бота
-  **[scripts/README.md](scripts/README.md)** - Документация скриптов деплоя

### 🧪 Тестовые файлы

-  **[frontend-example.html](frontend-example.html)** - Пример фронтенда
-  **[test-telegram-ping.js](test-telegram-ping.js)** - Тест Telegram интеграции
-  **[tests/WFP_SMOKE.md](tests/WFP_SMOKE.md)** - WayForPay тесты

---

## **🆘 Поддержка и устранение неполадок**

### 🔧 Частые проблемы

#### **Проблема: Контейнеры не запускаются**

```bash
# Проверьте логи
docker-compose logs

# Проверьте .env файл
cat .env

# Перезапустите контейнеры
docker-compose down && docker-compose up -d
```

#### **Проблема: WayForPay callback не работает**

```bash
# Проверьте CORS настройки
grep ALLOWED_ORIGINS .env

# Проверьте логи backend
docker-compose logs backend

# Проверьте подпись
curl -X POST http://localhost:8080/api/payments/wayforpay/callback \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'
```

#### **Проблема: Telegram уведомления не приходят**

```bash
# Проверьте токен бота
grep TELEGRAM_BOT_TOKEN .env

# Тест отправки
node test-telegram-ping.js

# Проверьте логи
docker-compose logs backend | grep -i telegram
```

### 📞 Получение помощи

1. **Проверьте логи:** `docker-compose logs -f`
2. **Проверьте статус:** `docker-compose ps`
3. **Проверьте .env файл** на корректность
4. **Убедитесь в доступности** всех внешних сервисов
5. **Используйте скрипты диагностики:** `./scripts/check_status.sh`

---

## **📈 Статус проекта**

### ✅ **Полностью реализовано и протестировано:**

-  **🎨 WordPress интеграция** - тема `cv-theme` работает корректно
-  **⚡ Node.js API** - все endpoints функционируют
-  **💳 WayForPay интеграция** - полный flow оплаты протестирован
-  **📱 Telegram уведомления** - отправляются при создании заказа и после оплаты
-  **📁 Файловая система** - загрузка и отправка файлов работает
-  **📊 База данных** - миграции и хранение данных настроены
-  **🌐 Nginx конфигурация** - правильное проксирование API запросов
-  **🔒 CORS настройки** - работают для всех необходимых доменов
-  **🚀 VPS деплой** - автоматизированные скрипты развертывания
-  **🔐 SSL/HTTPS** - настроено через Cloudflare

### 🚀 **Готово к продакшн использованию**

Проект полностью готов для развертывания на продакшн сервере. Все компоненты
протестированы и работают корректно.

### 📋 **Последние тесты (Январь 2025):**

-  ✅ **Создание заказа через API** - новый и классический формат
-  ✅ **WayForPay payment data генерация** - корректные подписи
-  ✅ **Callback обработка** с проверкой подписи
-  ✅ **Обновление статуса заказа** на "paid"
-  ✅ **Telegram уведомления** после оплаты с файлами
-  ✅ **Nginx проксирование** /api/ запросов
-  ✅ **CORS для WayForPay** callback'ов
-  ✅ **SSL/HTTPS** через Cloudflare
-  ✅ **Автоматический деплой** через скрипты

---

## **🎯 Git Workflow**

### 🌿 Ветки

-  **`main`** - основная ветка для продакшн
-  **`test-payments`** - ветка для тестирования WayForPay ✅ **ПРОТЕСТИРОВАНА**
-  **`feature/*`** - ветки для новых функций

### 🔄 Рабочий процесс

```bash
# Создание новой ветки
git checkout -b feature/new-feature

# Коммит изменений
git add .
git commit -m "Описание изменений"

# Пуш в репозиторий
git push origin feature/new-feature

# Слияние с main
git checkout main
git merge feature/new-feature
git push origin main
```

---

## **📄 Лицензия**

Этот проект разработан для коммерческого использования. Все права защищены.

---

## **👥 Авторы**

-  **Разработка:** CV Project Team
-  **Интеграция WayForPay:** Полностью реализована и протестирована
-  **Telegram интеграция:** Настроена и работает
-  **VPS деплой:** Автоматизирован через скрипты

---

**🎉 Проект готов к использованию! Все компоненты протестированы и работают
корректно.**
