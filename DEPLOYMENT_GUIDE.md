# 🚀 Руководство по деплою CV-проекта

## 📋 Текущее состояние продакшена

**Дата последнего обновления:** Январь 2025  
**Статус:** ✅ Полностью работает  
**URL:** https://seafarer-cv.com  
**VPS:** 91.107.204.141

## 🔧 Конфигурация

### SSL сертификаты

-  **Источник:** Cloudflare Origin CA
-  **Сертификат:** `ssl-configs/cloudflare_cert.txt`
-  **Приватный ключ:** `ssl-configs/cloudflare_private_key.txt`
-  **Статус:** ✅ Настроен и работает

### Nginx конфигурация

-  **Файл:** `nginx/conf.d/default.conf`
-  **Рабочая копия:** `ssl-configs/nginx_https_cloudflare_working.conf`
-  **Порты:** 80 (HTTP) + 443 (HTTPS)
-  **SSL:** Cloudflare Origin CA сертификаты
-  **Статус:** ✅ Оптимизирован для Grade A

### Переменные окружения

-  **Продакшн файл:** `.env` (на VPS)
-  **Пример:** `env.production.example`
-  **Статус:** ✅ Настроены реальные пароли и токены

### База данных

-  **WordPress DB:** `wp_production`
-  **Orders DB:** `orders_production`
-  **Статус:** ✅ Работает, содержит актуальные данные

## 🚀 Процедура деплоя

### 📋 Доступные скрипты деплоя

| Скрипт                | Назначение          | Когда использовать              |
| --------------------- | ------------------- | ------------------------------- |
| `vps_deploy_rsync.sh` | **Основной деплой** | Ежедневные обновления           |
| `sync_theme.sh`       | Обновление темы     | Изменения только в WordPress    |
| `vps_clean_deploy.sh` | Полная очистка      | Первый деплой или переустановка |
| `check_status.sh`     | Проверка статуса    | Диагностика проблем             |

### 1. Подготовка

```bash
# Убедитесь, что вы в корневой директории проекта
cd /path/to/CV-project

# Проверьте, что все файлы на месте
ls -la ssl-configs/cloudflare_*.txt
ls -la nginx/conf.d/default.conf
ls -la env.production.example

# Проверьте статус git
git status
```

### 2. Основной деплой (рекомендуется)

```bash
# Запустите основной скрипт деплоя
./scripts/vps_deploy_rsync.sh
```

**Что делает скрипт:**

-  ✅ Останавливает контейнеры (`docker-compose down`)
-  ✅ Синхронизирует все файлы через `rsync`
-  ✅ Запускает контейнеры (`docker-compose up -d --build`)
-  ✅ Проверяет статус и логи
-  ✅ Проверяет здоровье сервисов

### 3. Быстрое обновление темы

```bash
# Если изменили только тему WordPress
./scripts/sync_theme.sh
```

### 4. Полная переустановка

```bash
# ВНИМАНИЕ: Удалит ВСЕ данные!
./scripts/vps_clean_deploy.sh
```

### 5. Проверка после деплоя

```bash
# Автоматическая проверка статуса
./scripts/check_status.sh

# Ручная проверка контейнеров
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose ps"

# Проверка логов
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose logs --tail=10 nginx"
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose logs --tail=10 backend"
```

### 6. Тестирование функциональности

```bash
# Проверка API здоровья
curl https://seafarer-cv.com/api/health

# Проверка WordPress
curl https://seafarer-cv.com/wp-json/wp/v2/posts?per_page=1

# Проверка SSL
curl -I https://seafarer-cv.com

# Тест Telegram уведомлений
node test-telegram-ping.js

# Проверка базы данных
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose exec -T orders_db mysql -u orders_user -pyour_secure_orders_password orders_production -e 'SELECT COUNT(*) FROM orders;'"
```

## 🔄 Синхронизация с продакшена

### 📥 Копирование конфигурации с VPS

```bash
# Nginx конфигурация
scp root@91.107.204.141:/var/www/cv-project/nginx/conf.d/default.conf nginx/conf.d/default.conf

# SSL сертификаты
scp root@91.107.204.141:/var/www/cv-project/ssl/cloudflare_cert.pem ssl-configs/cloudflare_cert.txt
scp root@91.107.204.141:/var/www/cv-project/ssl/cloudflare_private_key.pem ssl-configs/cloudflare_private_key.txt

# Переменные окружения (только для анализа, не коммитить!)
scp root@91.107.204.141:/var/www/cv-project/.env env.production
```

### 📤 Отправка изменений на VPS

```bash
# Основной деплой (рекомендуется)
./scripts/vps_deploy_rsync.sh

# Только тема WordPress
./scripts/sync_theme.sh

# Только SSL сертификаты
./scripts/sync_ssl_certs.sh
```

## 🛠️ Структура проекта

```
CV-project/
├── backend/                           # Node.js API
│   ├── src/
│   │   ├── index.js                  # Основной API сервер
│   │   ├── db.js                     # Подключение к БД
│   │   ├── migrate.js                # Миграции БД
│   │   ├── telegram.js              # Telegram интеграция
│   │   └── payments/
│   │       ├── wayforpay.js         # WayForPay обработчик
│   │       └── index.js             # Экспорт модулей
│   ├── uploads/                      # Загруженные файлы
│   └── Dockerfile
├── wp-content/
│   └── themes/
│       └── cv-theme/                 # WordPress тема
├── nginx/
│   └── conf.d/
│       ├── default.conf              # Активная конфигурация
│       ├── default.conf.https        # HTTPS конфигурация
│       └── local.conf               # Локальная конфигурация
├── ssl-configs/                      # SSL сертификаты и конфигурации
│   ├── cloudflare_cert.txt          # Cloudflare сертификат
│   ├── cloudflare_private_key.txt   # Приватный ключ
│   ├── nginx_https_cloudflare_working.conf # Рабочая конфигурация
│   └── README.md                    # Документация SSL
├── scripts/                          # Скрипты деплоя и управления
│   ├── vps_deploy_rsync.sh          # Основной скрипт деплоя
│   ├── sync_theme.sh                # Синхронизация темы
│   ├── check_status.sh              # Проверка статуса
│   ├── deploy_ssl.sh                # Деплой SSL
│   └── README.md                    # Документация скриптов
├── env.production.example            # Пример переменных окружения
├── test-telegram-ping.js            # Тест Telegram интеграции
└── docker-compose.yml               # Docker конфигурация
```

## 🔍 Диагностика проблем

### Ошибка 521 (Web server is down)

1. Проверьте статус контейнеров: `docker-compose ps`
2. Проверьте логи nginx: `docker-compose logs nginx`
3. Убедитесь, что SSL сертификаты на месте
4. Проверьте конфигурацию nginx

### Проблемы с SSL

1. Проверьте наличие сертификатов в `ssl/`
2. Убедитесь, что пути в nginx.conf правильные
3. Проверьте права доступа к файлам

### Проблемы с API

1. Проверьте логи backend: `docker-compose logs backend`
2. Убедитесь, что база данных запущена
3. Проверьте переменные окружения

## 🔍 Диагностика проблем

### 🚨 Частые проблемы и решения

#### **Ошибка 521 (Web server is down)**

```bash
# 1. Проверьте статус контейнеров
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose ps"

# 2. Проверьте логи nginx
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose logs nginx"

# 3. Перезапустите контейнеры
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose restart nginx"
```

#### **Проблемы с SSL**

```bash
# 1. Проверьте наличие сертификатов
ls -la ssl-configs/cloudflare_*.txt

# 2. Проверьте права доступа
ssh root@91.107.204.141 "ls -la /var/www/cv-project/ssl/"

# 3. Обновите SSL сертификаты
./scripts/deploy_ssl.sh
```

#### **Проблемы с API**

```bash
# 1. Проверьте логи backend
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose logs backend"

# 2. Проверьте базу данных
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose exec orders_db mysql -u orders_user -pyour_secure_orders_password orders_production -e 'SHOW TABLES;'"

# 3. Проверьте переменные окружения
ssh root@91.107.204.141 "cd /var/www/cv-project && grep TELEGRAM_BOT_TOKEN .env"
```

#### **Проблемы с Telegram**

```bash
# 1. Тест отправки сообщения
node test-telegram-ping.js

# 2. Проверьте токен в логах
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose logs backend | grep -i telegram"

# 3. Проверьте переменные окружения
ssh root@91.107.204.141 "cd /var/www/cv-project && grep TELEGRAM .env"
```

#### **Проблемы с WayForPay**

```bash
# 1. Проверьте логи callback'ов
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose logs backend | grep -i wayforpay"

# 2. Проверьте настройки
ssh root@91.107.204.141 "cd /var/www/cv-project && grep WAYFORPAY .env"

# 3. Тест callback endpoint
curl -X POST https://seafarer-cv.com/api/payments/wayforpay/callback \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'
```

### 🔧 Автоматическая диагностика

```bash
# Полная проверка статуса
./scripts/check_status.sh

# Проверка SSL рейтинга
./scripts/check_ssl_labs.sh

# Сравнение конфигураций
./scripts/compare_configs.sh
```

## 📊 Мониторинг

### 📋 Регулярные проверки

```bash
# Ежедневно - проверка статуса
./scripts/check_status.sh

# Еженедельно - проверка SSL
./scripts/check_ssl_labs.sh

# При проблемах - полная диагностика
ssh root@91.107.204.141 "cd /var/www/cv-project && docker-compose logs --tail=50"
```

### 📈 Метрики для отслеживания

-  **Статус контейнеров:** Все должны быть `Up`
-  **SSL рейтинг:** Grade A от SSL Labs
-  **API ответы:** `/api/health` должен возвращать `200`
-  **WordPress:** Главная страница должна загружаться
-  **База данных:** Заказы должны сохраняться
-  **Telegram:** Уведомления должны приходить
-  **WayForPay:** Callback'и должны обрабатываться

## 🔐 Безопасность

### ✅ Реализованные меры

-  **SSL сертификаты** от Cloudflare Origin CA
-  **HTTPS принудительно** включен
-  **CORS настроен** для домена seafarer-cv.com
-  **Безопасные заголовки** настроены в Nginx
-  **Пароли в переменных** окружения
-  **Защита от SQL инъекций** через prepared statements
-  **Валидация данных** на всех этапах

### 🛡️ Рекомендации

1. **Регулярно обновляйте** SSL сертификаты
2. **Мониторьте логи** на предмет подозрительной активности
3. **Создавайте бэкапы** баз данных
4. **Используйте сильные пароли** для всех сервисов
5. **Настройте firewall** на VPS

## 📝 Чек-лист перед деплоем

### ✅ Подготовка

-  [ ] Все изменения закоммичены в git
-  [ ] SSL сертификаты актуальны
-  [ ] Nginx конфигурация проверена
-  [ ] Переменные окружения настроены
-  [ ] Тесты проходят локально
-  [ ] Резервная копия создана

### ✅ После деплоя

-  [ ] Сайт доступен по HTTPS
-  [ ] API отвечает корректно (`/api/health`)
-  [ ] WordPress работает
-  [ ] Платежи функционируют
-  [ ] Telegram уведомления приходят
-  [ ] Логи не содержат ошибок
-  [ ] SSL рейтинг Grade A

## 📞 Контакты и информация

### 🌐 Основные данные

-  **VPS:** 91.107.204.141
-  **Домен:** seafarer-cv.com
-  **Cloudflare:** Настроен с Origin CA сертификатами
-  **Статус:** ✅ Полностью работает

### 🔧 Полезные команды

```bash
# Подключение к VPS
ssh root@91.107.204.141

# Переход в директорию проекта
cd /var/www/cv-project

# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Перезапуск сервисов
docker-compose restart

# Остановка проекта
docker-compose down

# Запуск проекта
docker-compose up -d
```

### 📚 Дополнительная документация

-  **[README.md](README.md)** - Основная документация проекта
-  **[API_DOCUMENTATION.md](API_DOCUMENTATION.md)** - Документация API
-  **[CLOUDFLARE_SETUP.md](CLOUDFLARE_SETUP.md)** - Настройка Cloudflare
-  **[TELEGRAM_SETUP.md](TELEGRAM_SETUP.md)** - Настройка Telegram
-  **[scripts/README.md](scripts/README.md)** - Документация скриптов

---

## 🎯 Краткое резюме

**DEPLOYMENT_GUIDE.md** содержит полную информацию для развертывания и
управления CV-проектом на VPS. Все процедуры протестированы и работают
корректно.

**Основные скрипты:**

-  `vps_deploy_rsync.sh` - основной деплой
-  `sync_theme.sh` - обновление темы
-  `check_status.sh` - проверка статуса
-  `deploy_ssl.sh` - обновление SSL

**Статус проекта:** ✅ Готов к продакшн использованию
